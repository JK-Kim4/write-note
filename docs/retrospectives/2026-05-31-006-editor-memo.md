# 006 Week3+4 — 에디터·원고지 + 메모 캡처 (advisor + Sonnet 위임 구현)

- 일자: 2026-05-31
- 워크트리 / 브랜치: 메인 / `006-phase-3-4-editor-memo`
- 관련 커밋: `364485e`(spec) → `47a82be`(plan) → `4002699`(tasks) → `34e5004`(US1) → `26b55b0`(US2) → `88275b2`(US3) → `f0ed503`(US4) → `28e1bbd`(US5) → `daf4628`(Polish)
- 작업 시간 (대략): 전 사이클 (specify ~ Polish) 1 세션

## 1. 무엇을 했는가 (사실)

- speckit 전 사이클: specify(spec.md US1~5/FR24/SC8/Out of Scope) → clarify(본문 nested 조회 경로 1건) → plan(research R-1~16 / data-model / contracts 3 / quickstart) → tasks(79) → implement.
- spec 결정과 모순된 `00-stack §7` / `01-phase 3-8` 의 "last-write-wins" 표기를 03-backend §6 공식 결정(②-8 C, 사용자 선택 UI)에 정합 정정.
- US1: DocumentController/Service(nested 조회 + 자동저장 PUT 409 + title) + TipTap 에디터 + useAutoSave(800ms+409) + 자수/진행률. backend 22 + frontend 신규 12 테스트.
- US2: 원고지 매수 계산(manuscript.ts) + CSS 격자 오버레이(ManuscriptGrid) + write page 모드 분기. frontend 11 테스트.
- US3: V6 마이그레이션(memos/memo_projects/memo_project_characters/api_tokens 4 테이블) + entity 4 + 캡처(데스크탑·모바일 멱등) + ApiTokenFilter stub 실제 결선 + ⌘+N 모달/inbox. backend 16 테스트.
- US4: MemoCurationService 차이계산 + N+1 회피(@EntityGraph) + 무결성 validator + M1/M2/M4/M5/M7 + 큐레이션 카드/필터. backend 21 + frontend 12 테스트.
- US5: ApiTokenService(발급 1회표시/해지) + Controller T1~T4 + /api/auth/me 활성 토큰 수 결선 + 설정 토큰 UI.
- Polish: OpenAPI annotation + 03-backend §6/§3-4/§3-7 + 02-progress §0-A(39/56≈70%) 갱신.
- 매 라운드 backend 전체 게이트(ktlint+checkstyle+test+build) advisor 직접 실행 — 전부 GREEN, 003/004/005 회귀 0.

## 2. 어떻게 했는가 (접근)

- 사용자 지정 방식: **Opus advisor(작업 단위 정의 / dispatch prompt / 결과 검토 / 빌드·테스트 직접 실행 / 엣지 보강) + Sonnet subagent(실제 코드 작성)**.
- R-16 MVP 라운드 분해: US1(MVP) → US2 → US3 → US4 → US5 → Polish. 각 라운드 = backend subagent → advisor 검토(전체 게이트 + 코드 정독 + 엣지) → frontend subagent → 검토 → 커밋.
- backend 는 gradle daemon 충돌 회피 위해 순차 위임(병렬 X). subagent dispatch prompt 에 검증 cap / commit 금지 / tool_uses cap / "막히면 중단" / 기존 패턴 grep 의무 명시.
- V6 마이그레이션 로컬 docker postgres 적용은 외부 인프라 HARD-GATE → US3 진입 전 사용자 명시 컨펌 받음.

## 3. 잘 된 점

1) **advisor 검토가 subagent 가 놓친/오판한 실질 결함 2건을 차단** — 근거:
   - US1 `client.ts` 가 모든 409 를 `DOCUMENT_VERSION_CONFLICT` 로 오분류 → `EMAIL_ALREADY_REGISTERED(409)` 회원가입이 깨짐. subagent 는 이를 "기존 회귀"로 오판 보고했으나, advisor 가 `SignupEmailForm.test` 직접 재현 → 신규 회귀로 규명 → code 기준 분기 수정 → GREEN.
   - US3 멱등 캐시 key 가 `Idempotency-Key` 헤더만(userId 미포함) → cross-user 정보 노출. advisor 코드 정독으로 발견 → userId 격리 수정.
2) **매 라운드 backend 전체 게이트 직접 실행** — entity 양방향 추가(US4)·UserAuthConverter 변경(US5) 등 회귀 위험 변경에서 003/004/005 회귀 0 을 매번 확인.
3) **HARD-GATE 준수** — V6 적용 전 사용자 컨펌 / 빌드·테스트 포어그라운드 직접 / spec↔SoT 모순(last-write-wins) 정정.
4) **clarify 절제** — data-model 의 cascade 가 이미 답하는 질문(인물 연결 해제)은 묻지 않고, 진짜 모순(본문 조회 경로)만 1건 질문.

## 4. 어긋난 점

- **subagent 자기진단 오판 1건** — US1 frontend subagent 가 `SignupEmailForm` 테스트 실패를 "이 작업 이전부터 존재하는 기존 회귀"로 단정 보고. 실제는 본인이 추가한 client.ts 409 분기가 만든 신규 회귀. advisor 가 직접 재현 안 했으면 회귀가 merge 될 뻔. **회피 시점**: subagent 보고의 "기존 회귀" 주장을 무검증 수용하지 않고 직접 재현한 것이 차단점 — 이 신뢰 검증이 룰화 후보.
- **ktlint test 소스셋 위반 2회**(US3, US4) — subagent 가 main 만 `ktlintFormat` 하고 test 빠뜨려 전체 게이트에서 chain-method-continuation 위반 발견 → advisor 가 `ktlintFormat` 후처리. dispatch prompt 에 "main+test 둘 다 format" 명시했으면 회피.
- **git index.lock stale 1회** — sed 마킹 + 빠른 연속 git 명령 중 lock 잔존(빈 파일, 21분 전). 프로세스 확인 후 제거. 큰 영향 없었으나 연속 git 명령 간 간섭 신호.
- **subagent 가 tasks 외 추가 service 분리**(US4: MemoQueryService/MemoEditService 신설) — tasks 엔 MemoCurationService 만 명시. 단일 책임 분리라 결과는 적절했으나 tasks 명세와 차이. 사전 경고 없었음.
- 멈춤 신호: 사용자 "잠깐"/"왜" 류 0회. 매 체크포인트에서 사용자가 진행 방향 선택(통합 spec / US2 계속 / 끝까지). 디버깅 같은 에러 3+ 재시도 0회.

## 5. 다음 작업에 남길 교훈

### 5-1. 작업별 교훈 (이 프로젝트 한정)

- **공용 레이어(client.ts / SecurityConfig / 전역 필터) 를 건드리는 subagent 산출물은 advisor 가 도메인 횡단 회귀를 직접 재현 검증** — "기존 회귀"라는 subagent 자기진단은 신뢰 전 재현.
- **공용 HTTP 클라이언트의 status 분기는 status code 단독이 아니라 error.code 기준** — 같은 status(409)에 여러 도메인 에러(문서충돌/이메일중복/카카오) 공존.
- **멱등/캐시/임시저장 key 는 사용자 식별자 포함**(소유 격리 FR-024) — 단일 사용자 V1 이라도 보안 기본.
- Week 5 진입 시: 메모 핀(②-10 #36/#37) + 세션노트(③-6) — V6 에서 제외한 `pinned_document_id` 컬럼 ADD + TipTap custom mark(`memo-pin`) + ProseMirror step.mapping (PoC 0-1 IME 회귀 재검증 의무).
- dogfooding 미완(T022/T028/T049/T062/T071) — ⌘+N 브라우저 예약 제약 + 원고지 한글 칸 정렬이 실입력 검증 핵심.

### 5-2. 룰 갱신 후보 (사용자 컨펌 필요)

**후보 1 — subagent 자기진단 "기존 회귀" 무검증 수용 금지**
- (1) 대상: `.claude/rules/shared/agent-workflow-discipline.md` (신규 §7)
- (2) 본문: subagent 가 테스트 실패를 "기존 회귀/내 변경 아님"으로 보고하면, advisor 는 수용 전 (a) 해당 테스트를 직접 재현 (b) subagent 변경 파일과의 인과를 확인. 특히 공용 레이어(client/config/필터/공유 컴포넌트) 변경 라운드에서 의무.
- (3) 근거: 본 회고 §4 — US1 client.ts 409 오분류를 subagent 가 "기존 회귀"로 오판, advisor 직접 재현으로 신규 회귀 규명.

**후보 2 — 공용 HTTP 클라이언트 status 분기는 error.code 기준**
- (1) 대상: `.claude/rules/typescript/code-quality.md` (§React/Next.js 또는 신규 §API 클라이언트)
- (2) 본문: 공용 fetch 래퍼에서 HTTP status(409/400 등)로 분기할 때, 같은 status 에 여러 도메인 에러코드가 공존하므로 status 단독 분기 금지 — 응답 `error.code` 로 분기. 신규 status 분기 추가 시 해당 status 를 쓰는 기존 에러코드 grep 의무.
- (3) 근거: §4 — 409 가 DOCUMENT_VERSION_CONFLICT / EMAIL_ALREADY_REGISTERED / KAKAO_ALREADY_LINKED 공유, status 분기가 회원가입 깨뜨림.

**후보 3 — subagent dispatch 시 ktlintFormat main+test 명시**
- (1) 대상: `.claude/rules/shared/agent-workflow-discipline.md` §4 (Dispatch 체크리스트)
- (2) 본문: backend Kotlin subagent dispatch prompt 에 "작성 후 `./gradlew ktlintFormat`(main+test 양쪽)" 명시 — test 소스셋 누락 시 전체 게이트에서 chain-method-continuation 등 위반.
- (3) 근거: §4 — US3/US4 ktlint test 위반 2회, advisor 후처리.

**후보 4 — 멱등/캐시 key 사용자 격리** (이미 external-infra-safety 인접하나 명문화 후보)
- (1) 대상: `.claude/rules/infra/external-infra-safety.md` 또는 `kotlin/code-quality.md`
- (2) 본문: 멱등성 키/메모리 캐시/임시 저장의 key 는 사용자 식별자를 포함(cross-user 충돌·정보 노출 방지). 소유 격리(RLS 대체) 정책의 캐시 레이어 확장.
- (3) 근거: §4·§3 — US3 IdempotencyCache cross-user 갭.

**사용자 컨펌 전까지 실제 룰 파일 수정 금지.**
