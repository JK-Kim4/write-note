# R7 Export 구현 계획

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 작품의 챕터들을 골라 묶어 PDF(클라 인쇄)·HWPX·DOCX(백엔드 생성)로 내보낸다.

**Architecture:** 공통 수집(`collectChapters`)·다이얼로그(`ExportDialog`) 위에 세 경로 — PDF 는 자체 엔진 `relayout`(measure→layout) 재사용 후 페이지별 인쇄 DOM + `window.print()`(백엔드 0), HWPX/DOCX 는 프론트가 `ExportDoc` DTO 로 변환해 백엔드 `/api/export/*` 로 POST → hwpxlib/Apache POI 생성 → blob 다운로드.

**Tech Stack:** Next.js 16 + React 19 + TypeScript(프론트), Kotlin + Spring Boot(백엔드), `kr.dogfoot:hwpxlib:1.0.5`, `org.apache.poi:poi-ooxml`. 테스트: Vitest(프론트), JUnit5+AssertJ+MockK+MockMvc(백엔드).

**중요 — spec 정정:** spec `§4-2` 의 "PaperSize 를 `custom-editor/geometry` 로 통일" 은 부정확하다. 실제로 자체 엔진 결선(`BCustomChapterEditor.tsx:31-35`)에 이미 `toGeoPaperSize(size: LayoutPaperSize): GeoPaperSize` 매핑이 있고(A4/A3/A2/B4 동일 문자열), PDF export 는 이 매핑을 그대로 재사용한다. **PaperSize 통일 작업은 없다.**

---

## File Structure

**생성(프론트):**
- `frontend/src/lib/export/exportDoc.ts` — `ExportDoc`/`ExportChapter`/`ExportBlock` 타입 + `docModelToExportBlocks`(DocModel→ExportBlock[]) + `buildExportDoc`(워드 경로 DTO)
- `frontend/src/lib/export/mergeChapters.ts` — `mergeChaptersForPrint`(CollectedChapter[]+joinMode → DocModel[], PDF 합본 규칙)
- `frontend/src/components/custom-editor/printLayout.tsx` — CustomEditor 에서 추출한 `layoutForPrint`(relayout) + `renderRuns` 공유
- `frontend/src/components/export/PrintDocument.tsx` — DocModel[] → 인쇄 페이지 DOM(break-after)
- `frontend/src/components/export/PrintOverlay.tsx` — print 마운트→`window.print()`→afterprint 언마운트
- `frontend/src/lib/api/export.ts` — 백엔드 워드 export 호출 + blob 다운로드
- `frontend/src/app/globals.css` 또는 전용 CSS — `.export-dialog` + `@media print` 규칙

**수정(프론트):**
- `frontend/src/components/export/ExportDialog.tsx` — 포맷 3개 활성·joinMode 셀렉트·`onExportWord`
- `frontend/src/components/custom-editor/CustomEditor.tsx` — `relayout`/`renderRuns` 를 `printLayout.tsx` 로 추출 후 import
- `frontend/src/components/b/BStudioShell.tsx:535-542` — `onExportPdf`/`onExportWord` 결선

**생성(백엔드):**
- `backend/src/main/kotlin/com/writenote/model/request/ExportRequest.kt` — `ExportDoc` DTO
- `backend/src/main/kotlin/com/writenote/service/DocxExportService.kt` — POI 생성
- `backend/src/main/kotlin/com/writenote/service/HwpxExportService.kt` — hwpxlib 생성(스파이크 승격)
- `backend/src/main/kotlin/com/writenote/controller/ExportController.kt` — 2 endpoint
- 테스트: `DocxExportServiceTest.kt`, `HwpxExportServiceTest.kt`, `ExportControllerWebTest.kt`

**수정(백엔드):**
- `backend/build.gradle.kts:24-41` — POI `implementation` 추가, hwpxlib `testImplementation`→`implementation`

---

## ExportDoc 타입 계약 (전 Phase 공유)

```ts
// frontend/src/lib/export/exportDoc.ts
export type ExportMark = { start: number; end: number; bold?: boolean; italic?: boolean; underline?: boolean; strike?: boolean };
export type ExportBlock = {
  type: "paragraph" | "heading" | "blockquote" | "listItem" | "hr";
  level?: 1 | 2 | 3;                     // heading
  listKind?: "bullet" | "ordered";        // listItem
  depth?: number;                         // listItem
  text: string;                           // 소프트 줄바꿈 U+2028 → \n 정규화
  marks: ExportMark[];                    // text(정규화 후) 기준 offset
};
export type ExportChapter = { title: string; blocks: ExportBlock[] };
export type JoinMode = "page-title" | "inline-title" | "body-only";
export type ExportDoc = { paperSize: string; joinMode: JoinMode; chapters: ExportChapter[] };
```

백엔드 Kotlin DTO 는 위와 1:1(§Task 8).

---

# Phase 0 — 공통 다이얼로그·CSS (프론트)

## Task 1: ExportDialog 확장 (포맷 활성 + joinMode + onExportWord)

**Files:**
- Modify: `frontend/src/components/export/ExportDialog.tsx`
- Test: `frontend/src/components/export/ExportDialog.test.tsx` (생성)

- [ ] **Step 1: 실패하는 테스트 작성**

```tsx
// frontend/src/components/export/ExportDialog.test.tsx
import { render, screen, fireEvent } from "@testing-library/react";
import { describe, it, expect, vi } from "vitest";
import { ExportDialog } from "./ExportDialog";
import type { ChapterMetaResponse } from "@/types/api";

const chapters: ChapterMetaResponse[] = [
  { id: 1, projectId: 9, title: "1장", sortOrder: 0, wordCount: 100, updatedAt: "2026-06-16T00:00:00Z" },
  { id: 2, projectId: 9, title: "2장", sortOrder: 1, wordCount: 200, updatedAt: "2026-06-16T00:00:00Z" },
];

describe("ExportDialog", () => {
  it("HWPX·DOCX 포맷 버튼이 활성화된다", () => {
    render(<ExportDialog open chapters={chapters} paperSize="A4" onExportPdf={vi.fn()} onExportWord={vi.fn()} onClose={vi.fn()} />);
    expect(screen.getByRole("button", { name: "HWPX" })).toBeEnabled();
    expect(screen.getByRole("button", { name: "DOCX" })).toBeEnabled();
  });

  it("HWPX 선택 후 내보내기 시 onExportWord 가 format·joinMode 와 함께 호출된다", () => {
    const onExportWord = vi.fn();
    render(<ExportDialog open chapters={chapters} paperSize="A4" onExportPdf={vi.fn()} onExportWord={onExportWord} onClose={vi.fn()} />);
    fireEvent.click(screen.getByRole("button", { name: "HWPX" }));
    fireEvent.click(screen.getByRole("button", { name: "내보내기" }));
    expect(onExportWord).toHaveBeenCalledWith("hwpx", { orderedIds: [1, 2], lined: false, joinMode: "page-title" });
  });

  it("합본 모드 셀렉트를 바꾸면 요청 joinMode 가 반영된다", () => {
    const onExportPdf = vi.fn();
    render(<ExportDialog open chapters={chapters} paperSize="A4" onExportPdf={onExportPdf} onExportWord={vi.fn()} onClose={vi.fn()} />);
    fireEvent.change(screen.getByLabelText("합본 방식"), { target: { value: "body-only" } });
    fireEvent.click(screen.getByRole("button", { name: "내보내기" }));
    expect(onExportPdf).toHaveBeenCalledWith({ orderedIds: [1, 2], lined: false, joinMode: "body-only" });
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/components/export/ExportDialog.test.tsx`
Expected: FAIL — `onExportWord` prop 없음 / HWPX 버튼 disabled.

- [ ] **Step 3: ExportDialog 수정**

```tsx
// frontend/src/components/export/ExportDialog.tsx 전체 교체
"use client";

import { useState } from "react";
import type { ChapterMetaResponse } from "@/types/api";
import type { PaperSize } from "@/components/editor/pageLayout";
import type { JoinMode } from "@/lib/export/exportDoc";

export type ExportRequest = { orderedIds: number[]; lined: boolean; joinMode: JoinMode };

type ExportDialogProps = {
    open: boolean;
    chapters: ChapterMetaResponse[];
    paperSize: PaperSize;
    onExportPdf: (req: ExportRequest) => void;
    onExportWord: (format: "hwpx" | "docx", req: ExportRequest) => void;
    onClose: () => void;
};

type Format = "pdf" | "hwpx" | "docx";
const FORMATS = ["pdf", "hwpx", "docx"] as const;
const JOIN_MODES: { value: JoinMode; label: string }[] = [
    { value: "page-title", label: "챕터마다 새 페이지 + 제목" },
    { value: "inline-title", label: "연속 + 챕터 제목" },
    { value: "body-only", label: "제목 없이 본문만" },
];

/**
 * Export 설정창(presentational). 부모는 `{open && <ExportDialog .../>}` 조건부 마운트(stale 방지).
 */
export function ExportDialog({ open, chapters, paperSize, onExportPdf, onExportWord, onClose }: ExportDialogProps) {
    const [order, setOrder] = useState<number[]>(() => chapters.map((c) => c.id));
    const [selected, setSelected] = useState<Set<number>>(() => new Set(chapters.map((c) => c.id)));
    const [format, setFormat] = useState<Format>("pdf");
    const [lined, setLined] = useState(false);
    const [joinMode, setJoinMode] = useState<JoinMode>("page-title");

    if (!open) return null;

    const toggle = (id: number) =>
        setSelected((prev) => {
            const next = new Set(prev);
            if (next.has(id)) next.delete(id);
            else next.add(id);
            return next;
        });

    const move = (id: number, dir: -1 | 1) =>
        setOrder((prev) => {
            const i = prev.indexOf(id);
            const j = i + dir;
            if (j < 0 || j >= prev.length) return prev;
            const next = [...prev];
            [next[i], next[j]] = [next[j], next[i]];
            return next;
        });

    const orderedSelected = order.filter((id) => selected.has(id));
    const canExport = orderedSelected.length > 0;
    const byId = new Map(chapters.map((c) => [c.id, c]));

    const handleExport = () => {
        if (!canExport) return;
        const req = { orderedIds: orderedSelected, lined, joinMode };
        if (format === "pdf") onExportPdf(req);
        else onExportWord(format, req);
    };

    return (
        <div role="dialog" aria-label="내보내기" className="export-dialog">
            <div className="export-dialog__panel">
                <div className="export-dialog__formats">
                    {FORMATS.map((f) => (
                        <button key={f} type="button" aria-pressed={format === f} onClick={() => setFormat(f)}>
                            {f.toUpperCase()}
                        </button>
                    ))}
                </div>
                <ul className="export-dialog__chapters">
                    {order.map((id) => {
                        const c = byId.get(id);
                        if (!c) return null;
                        return (
                            <li key={id}>
                                <label>
                                    <input type="checkbox" checked={selected.has(id)} onChange={() => toggle(id)} aria-label={`${c.title} 포함`} />
                                    {c.title}
                                </label>
                                <span>{c.wordCount.toLocaleString()}자</span>
                                <button type="button" aria-label={`${c.title} 위로`} onClick={() => move(id, -1)}>⌃</button>
                                <button type="button" aria-label={`${c.title} 아래로`} onClick={() => move(id, 1)}>⌄</button>
                            </li>
                        );
                    })}
                </ul>
                <label className="export-dialog__option">
                    합본 방식
                    <select aria-label="합본 방식" value={joinMode} onChange={(e) => setJoinMode(e.target.value as JoinMode)}>
                        {JOIN_MODES.map((m) => (
                            <option key={m.value} value={m.value}>{m.label}</option>
                        ))}
                    </select>
                </label>
                <label className="export-dialog__option">
                    <input type="checkbox" checked={lined} onChange={(e) => setLined(e.target.checked)} />
                    줄노트 줄 포함
                </label>
                <p className="export-dialog__paper">용지 {paperSize} (작품 설정)</p>
                <div className="export-dialog__actions">
                    <button type="button" onClick={handleExport} disabled={!canExport}>내보내기</button>
                    <button type="button" onClick={onClose}>닫기</button>
                </div>
            </div>
        </div>
    );
}
```

> 주의: `JoinMode` import 가 Task 7 의 `exportDoc.ts` 에 의존한다. Task 1 을 먼저 하면 `exportDoc.ts` 가 없으니, 이 import 만 위해 Task 7 의 타입 블록(`ExportMark`~`ExportDoc`)을 `exportDoc.ts` 에 **먼저 생성**한다(변환 함수는 Task 7 에서 추가). 빈 파일 대신 타입만 있는 파일로 시작.

- [ ] **Step 4: 타입 파일 선행 생성**

```ts
// frontend/src/lib/export/exportDoc.ts (타입만, 함수는 Task 7)
export type ExportMark = { start: number; end: number; bold?: boolean; italic?: boolean; underline?: boolean; strike?: boolean };
export type ExportBlock = {
  type: "paragraph" | "heading" | "blockquote" | "listItem" | "hr";
  level?: 1 | 2 | 3;
  listKind?: "bullet" | "ordered";
  depth?: number;
  text: string;
  marks: ExportMark[];
};
export type ExportChapter = { title: string; blocks: ExportBlock[] };
export type JoinMode = "page-title" | "inline-title" | "body-only";
export type ExportDoc = { paperSize: string; joinMode: JoinMode; chapters: ExportChapter[] };
```

- [ ] **Step 5: 테스트 통과 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/components/export/ExportDialog.test.tsx && node_modules/.bin/tsc --noEmit`
Expected: PASS, 타입 에러 0.

- [ ] **Step 6: 커밋**

```bash
git add frontend/src/components/export/ExportDialog.tsx frontend/src/components/export/ExportDialog.test.tsx frontend/src/lib/export/exportDoc.ts
git commit -m "feat(023-r7): ExportDialog 3포맷 활성 + 합본 모드 셀렉트 + onExportWord"
```

## Task 2: `.export-dialog` CSS (모달 + 인쇄 규칙)

**Files:**
- Modify: `frontend/src/app/globals.css` (또는 프로젝트의 전역 CSS — 작업 전 `grep -rl "export-dialog\|@media print" frontend/src/app` 로 위치 확인)

> 시각 스타일이라 TDD 완화(§5-5). 빌드·육안 dogfooding 으로 검증.

- [ ] **Step 1: CSS 추가**

```css
/* Export 다이얼로그 — 모달 오버레이 */
.export-dialog {
  position: fixed; inset: 0; z-index: 50;
  display: flex; align-items: center; justify-content: center;
  background: rgba(0, 0, 0, 0.4);
}
.export-dialog__panel {
  background: #fff; border-radius: 12px; padding: 24px;
  width: min(440px, 92vw); max-height: 86vh; overflow: auto;
  box-shadow: 0 8px 32px rgba(0, 0, 0, 0.18);
  display: flex; flex-direction: column; gap: 16px;
}
.export-dialog__formats { display: flex; gap: 8px; }
.export-dialog__formats button { flex: 1; padding: 8px; border: 1px solid #d4d4d8; border-radius: 8px; background: #fff; }
.export-dialog__formats button[aria-pressed="true"] { border-color: #4f46e5; color: #4f46e5; font-weight: 600; }
.export-dialog__chapters { list-style: none; margin: 0; padding: 0; display: flex; flex-direction: column; gap: 6px; }
.export-dialog__chapters li { display: flex; align-items: center; gap: 8px; }
.export-dialog__chapters li label { flex: 1; display: flex; align-items: center; gap: 6px; }
.export-dialog__option { display: flex; align-items: center; gap: 8px; }
.export-dialog__actions { display: flex; gap: 8px; justify-content: flex-end; }
.export-dialog__paper { color: #6b7280; font-size: 13px; margin: 0; }

/* 인쇄 — PrintOverlay 만 보이게 (Task 5·6) */
@media print {
  body > *:not(.print-root) { display: none !important; }
  .print-root { display: block !important; }
}
```

- [ ] **Step 2: 빌드 확인 + 커밋**

Run: `cd frontend && pnpm build`
Expected: 빌드 성공.

```bash
git add frontend/src/app/globals.css
git commit -m "feat(023-r7): .export-dialog 모달 CSS + @media print 규칙"
```

---

# Phase 1 — PDF 생성 (프론트)

## Task 3: 합본 DocModel 헬퍼 `mergeChaptersForPrint`

joinMode 에 따라 챕터들을 PDF 렌더용 DocModel 배열로 만든다.
- `page-title`: 챕터마다 별도 DocModel(맨 앞에 챕터 제목을 heading level 1 블록으로 prepend) → 챕터별 페이지 분리.
- `inline-title`: 단일 DocModel(챕터 사이에 제목 heading 블록 삽입) — 1개 원소 배열.
- `body-only`: 단일 DocModel(제목 없이 본문만 이어붙임) — 1개 원소 배열.

**Files:**
- Create: `frontend/src/lib/export/mergeChapters.ts`
- Test: `frontend/src/lib/export/mergeChapters.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// frontend/src/lib/export/mergeChapters.test.ts
import { describe, it, expect } from "vitest";
import { mergeChaptersForPrint } from "./mergeChapters";
import type { CollectedChapter } from "./collectChapters";

// 본문 "가" 단일 문단 PM JSON
const pm = (t: string) => JSON.stringify({ type: "doc", content: [{ type: "paragraph", content: [{ type: "text", text: t }] }] });
const chapters: CollectedChapter[] = [
  { id: 1, title: "1장", bodyJson: pm("가나다") },
  { id: 2, title: "2장", bodyJson: pm("라마바") },
];

describe("mergeChaptersForPrint", () => {
  it("page-title: 챕터 수만큼 DocModel, 각 첫 블록이 제목 heading", () => {
    const models = mergeChaptersForPrint(chapters, "page-title");
    expect(models).toHaveLength(2);
    expect(models[0].blockAttrs[0]).toEqual({ type: "heading", level: 1 });
    expect(models[0].buffer.split("\n")[0]).toBe("1장");
    expect(models[1].buffer.split("\n")[0]).toBe("2장");
  });

  it("body-only: 단일 DocModel, 제목 블록 없음, 본문만 연결", () => {
    const models = mergeChaptersForPrint(chapters, "body-only");
    expect(models).toHaveLength(1);
    expect(models[0].buffer).toBe("가나다\n라마바");
    expect(models[0].blockAttrs.every((a) => a.type !== "heading")).toBe(true);
  });

  it("inline-title: 단일 DocModel, 챕터 제목이 heading 으로 사이에 삽입", () => {
    const models = mergeChaptersForPrint(chapters, "inline-title");
    expect(models).toHaveLength(1);
    const segs = models[0].buffer.split("\n");
    expect(segs[0]).toBe("1장");
    expect(models[0].blockAttrs[0]).toEqual({ type: "heading", level: 1 });
    expect(segs).toContain("2장");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/lib/export/mergeChapters.test.ts`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```ts
// frontend/src/lib/export/mergeChapters.ts
import type { CollectedChapter } from "./collectChapters";
import { pmJsonToModel } from "@/components/custom-editor/pmConvert";
import type { BlockAttr, DocModel, MarkRun } from "@/components/custom-editor/model";

/** 제목 heading(level 1) 블록 1개짜리 DocModel 조각. */
function titleBlock(title: string): { text: string; attr: BlockAttr; runs: MarkRun[] } {
  return { text: title, attr: { type: "heading", level: 1 }, runs: title.length > 0 ? [{ len: title.length, mask: 0 }] : [] };
}

/** DocModel 을 (text, attr, runs) 블록 배열로 분해. */
function toBlocks(model: DocModel): { text: string; attr: BlockAttr; runs: MarkRun[] }[] {
  const segs = model.buffer.split("\n");
  return segs.map((text, i) => ({
    text,
    attr: model.blockAttrs[i] ?? { type: "paragraph" },
    runs: model.markRuns[i] ?? [],
  }));
}

/** 블록 배열을 DocModel 로 조립. */
function fromBlocks(blocks: { text: string; attr: BlockAttr; runs: MarkRun[] }[]): DocModel {
  if (blocks.length === 0) return { buffer: "", blockAttrs: [{ type: "paragraph" }], markRuns: [[]] };
  return {
    buffer: blocks.map((b) => b.text).join("\n"),
    blockAttrs: blocks.map((b) => b.attr),
    markRuns: blocks.map((b) => b.runs),
  };
}

/**
 * joinMode 에 따라 챕터들을 PDF 렌더용 DocModel 배열로 변환.
 * page-title → 챕터당 1 DocModel(제목 prepend), 그 외 → 단일 DocModel 1개.
 */
export function mergeChaptersForPrint(chapters: CollectedChapter[], joinMode: "page-title" | "inline-title" | "body-only"): DocModel[] {
  if (joinMode === "page-title") {
    return chapters.map((c) => fromBlocks([titleBlock(c.title), ...toBlocks(pmJsonToModel(c.bodyJson))]));
  }
  const merged = chapters.flatMap((c) => {
    const body = toBlocks(pmJsonToModel(c.bodyJson));
    return joinMode === "inline-title" ? [titleBlock(c.title), ...body] : body;
  });
  return [fromBlocks(merged)];
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/lib/export/mergeChapters.test.ts`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/lib/export/mergeChapters.ts frontend/src/lib/export/mergeChapters.test.ts
git commit -m "feat(023-r7): mergeChaptersForPrint — joinMode별 PDF 합본 DocModel"
```

## Task 4: `relayout`/`renderRuns` 공유 모듈 추출

PDF 인쇄 렌더가 화면 에디터와 동일한 measure→layout 을 쓰도록 `CustomEditor.tsx` 의 `relayout`·`renderRuns`·`FONT_FAMILY`·`ParsedBlock`/`View` 타입을 `printLayout.tsx` 로 옮기고 CustomEditor 는 그것을 import 한다. **동작 보존 추출**(로직 변경 0) — 기존 page.test 무회귀로 가드.

**Files:**
- Create: `frontend/src/components/custom-editor/printLayout.tsx`
- Modify: `frontend/src/components/custom-editor/CustomEditor.tsx`

- [ ] **Step 1: `printLayout.tsx` 생성 — CustomEditor 에서 발췌**

CustomEditor.tsx 의 다음을 잘라 옮긴다(현재 코드 그대로, 로직 변경 금지):
- 상수 `FONT_FAMILY`(:53), `OBJ`/`IMG_*`/`IMG_SRC`(:54-66)
- 타입 `ParsedBlock`(:68-89), `View`(:91)
- 함수 `relayout`(:97-124) → **export** `relayout`
- 함수 `renderRuns`(text, marks) — CustomEditor 내 정의부를 찾아(`grep -n "renderRuns" CustomEditor.tsx`) export 로 이동

```tsx
// frontend/src/components/custom-editor/printLayout.tsx
"use client";
import { type ReactNode } from "react";
import { blockFont, type PageGeometry } from "./geometry";
import { layout, type LaidOutPage, type MeasuredBlock, type MeasuredLine } from "./layoutEngine";
import { blockIndentPx, measureParagraphLines } from "./measure";
import { blockRuns, listNumberAt, type BlockAttr, type DocModel, type MarkRun } from "./model";

export const FONT_FAMILY = "'Apple SD Gothic Neo', 'Noto Serif KR', serif";
export const OBJ = "￼";
const IMG_NW = 600;
const IMG_NH = 400;
export const IMG_SRC = /* CustomEditor.tsx:58-66 의 IMG_SRC 리터럴 그대로 */;

export type ParsedBlock = /* CustomEditor.tsx:68-89 그대로 */;
export type View = { blocks: ParsedBlock[]; pages: LaidOutPage[] };

export function relayout(model: DocModel, geo: PageGeometry): View {
  /* CustomEditor.tsx:97-124 본문 그대로 */
}

export function renderRuns(text: string, marks: MarkRun[]): ReactNode {
  /* CustomEditor.tsx 의 renderRuns 본문 그대로 */
}
```

- [ ] **Step 2: CustomEditor.tsx 수정 — 추출분을 import 로 교체**

CustomEditor.tsx 에서 위 발췌분 정의를 삭제하고 상단에 추가:
```tsx
import { FONT_FAMILY, OBJ, IMG_SRC, relayout, renderRuns, type ParsedBlock, type View } from "./printLayout";
```
(CustomEditor 내부의 `relayout`/`renderRuns`/`FONT_FAMILY`/`OBJ`/`IMG_SRC` 사용처는 변경 없음 — import 로 동일 심볼 제공.)

- [ ] **Step 3: 회귀 가드 — 기존 자체 엔진 테스트 전체 실행**

Run: `cd frontend && node_modules/.bin/vitest run src/components/custom-editor && node_modules/.bin/tsc --noEmit`
Expected: 기존 테스트 전부 PASS(추출 전과 동일), 타입 에러 0.

- [ ] **Step 4: 빌드 확인 + 커밋**

Run: `cd frontend && pnpm build`
Expected: 성공.

```bash
git add frontend/src/components/custom-editor/printLayout.tsx frontend/src/components/custom-editor/CustomEditor.tsx
git commit -m "refactor(023-r7): relayout/renderRuns 를 printLayout 으로 추출(동작 보존)"
```

## Task 5: `PrintDocument` 컴포넌트 (DocModel[] → 인쇄 페이지 DOM)

각 DocModel 을 `relayout` 으로 페이지화하고, 페이지마다 고정 크기(geo.pageWidthPx×pageHeightPx) div 를 `break-after: page` 로 렌더한다. caret/selection 없음(인쇄 전용). `lined` 면 괘선 배경.

**Files:**
- Create: `frontend/src/components/export/PrintDocument.tsx`
- Test: `frontend/src/components/export/PrintDocument.test.tsx`

- [ ] **Step 1: 실패하는 테스트 작성**

> jsdom 에서 `measureParagraphLines`(Range.getBoundingClientRect) 는 0 을 반환해 줄 분해가 비현실적이다. 따라서 **페이지 컨테이너 개수·@page 스타일·본문 텍스트 존재**만 검증(시각 정합은 PoC dogfooding).

```tsx
// frontend/src/components/export/PrintDocument.test.tsx
import { render, screen } from "@testing-library/react";
import { describe, it, expect } from "vitest";
import { PrintDocument } from "./PrintDocument";
import type { DocModel } from "@/components/custom-editor/model";

const model: DocModel = { buffer: "안녕하세요", blockAttrs: [{ type: "paragraph" }], markRuns: [[{ len: 5, mask: 0 }]] };

describe("PrintDocument", () => {
  it("DocModel 의 본문 텍스트를 렌더한다", () => {
    render(<PrintDocument models={[model]} paperSize="A4" lined={false} />);
    expect(screen.getByText("안녕하세요")).toBeInTheDocument();
  });

  it("print-root 컨테이너로 감싼다", () => {
    const { container } = render(<PrintDocument models={[model]} paperSize="A4" lined={false} />);
    expect(container.querySelector(".print-root")).not.toBeNull();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/components/export/PrintDocument.test.tsx`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: 구현**

```tsx
// frontend/src/components/export/PrintDocument.tsx
"use client";
import { Fragment } from "react";
import { pageGeometry } from "@/components/custom-editor/geometry";
import type { PaperSize as LayoutPaperSize } from "@/components/editor/pageLayout";
import { FONT_FAMILY, IMG_SRC, relayout, renderRuns, type ParsedBlock } from "@/components/custom-editor/printLayout";
import type { DocModel } from "@/components/custom-editor/model";

const FONT_SIZE_PX = 18;
const MARGIN_MM = 25;
const MM_TO_PX = 96 / 25.4;

type Props = { models: DocModel[]; paperSize: LayoutPaperSize; lined: boolean };

/**
 * 인쇄 전용 문서 렌더. models 각각을 relayout 으로 페이지화해 모든 페이지를 break-after:page 로 잇는다.
 * geometry·FONT_FAMILY·FONT_SIZE_PX 는 화면 에디터(BCustomChapterEditor)와 동일 — 줄·페이지 정합.
 */
export function PrintDocument({ models, paperSize, lined }: Props) {
  const geo = pageGeometry(paperSize as never, FONT_SIZE_PX);
  const marginPx = MARGIN_MM * MM_TO_PX;
  // 모든 모델의 페이지를 평탄화(모델 경계 = 새 페이지 시작이 자연 보장: 모델마다 relayout 독립).
  const docPages = models.flatMap((model) => {
    const view = relayout(model, geo);
    return view.pages.map((page) => ({ page, byId: Object.fromEntries(view.blocks.map((b) => [b.id, b])) }));
  });

  return (
    <div className="print-root">
      <style>{`@page { size: ${paperSize} portrait; margin: 0; }`}</style>
      {docPages.map(({ page, byId }, pi) => (
        <div
          key={pi}
          style={{
            position: "relative", width: geo.pageWidthPx, height: geo.pageHeightPx, background: "#fff",
            breakAfter: pi < docPages.length - 1 ? "page" : "auto", overflow: "hidden",
          }}
        >
          {lined && <LinedBackground geo={geo} marginPx={marginPx} />}
          <div style={{ position: "absolute", left: marginPx, top: marginPx, width: geo.contentWidthPx, height: geo.contentHeightPx }}>
            {page.fragments.map((f, idx) => {
              const b = byId[f.blockId] as ParsedBlock | undefined;
              if (f.kind === "image" && b?.kind === "image") {
                // eslint-disable-next-line @next/next/no-img-element
                return <img key={idx} src={IMG_SRC} alt="" style={{ position: "absolute", top: f.offsetY, left: 0, height: f.height, width: "auto", maxWidth: geo.contentWidthPx }} />;
              }
              if (f.kind === "paragraph" && b?.kind === "paragraph") {
                if (b.attr.type === "hr") {
                  return (
                    <div key={idx} style={{ position: "absolute", top: f.offsetY, left: 0, width: geo.contentWidthPx, height: f.height, display: "flex", alignItems: "center" }}>
                      <div style={{ width: "100%", borderTop: "2px solid #d4d4d8" }} />
                    </div>
                  );
                }
                const isQuote = b.attr.type === "blockquote";
                const isList = b.attr.type === "listItem";
                const marker = isList && f.startLine === 0 ? (b.listNumber != null ? `${b.listNumber}.` : "•") : null;
                return (
                  <div key={idx} style={{ position: "absolute", top: f.offsetY, left: 0, width: geo.contentWidthPx, height: f.height, overflow: "hidden" }}>
                    {isQuote && <div style={{ position: "absolute", left: 0, top: 0, bottom: 0, width: 3, background: "#a1a1aa" }} />}
                    {marker != null && (
                      <div style={{ position: "absolute", left: 0, top: 0, width: b.indentPx, fontSize: b.fontSizePx, lineHeight: `${b.lineHeightPx}px`, fontFamily: FONT_FAMILY, color: "#6b7280", boxSizing: "border-box", textAlign: b.listNumber != null ? "right" : "center", paddingRight: b.listNumber != null ? 6 : 0 }}>
                        {marker}
                      </div>
                    )}
                    <div style={{ transform: `translateY(${-(f.startLine * b.lineHeightPx)}px)`, width: geo.contentWidthPx, paddingLeft: b.indentPx, boxSizing: "border-box", fontSize: b.fontSizePx, lineHeight: `${b.lineHeightPx}px`, fontFamily: FONT_FAMILY, whiteSpace: "pre-wrap", wordBreak: "break-word", color: "#1f2937" }}>
                      {renderRuns(b.text, b.marks)}
                    </div>
                  </div>
                );
              }
              return null;
            })}
          </div>
        </div>
      ))}
    </div>
  );
}

function LinedBackground({ geo, marginPx }: { geo: { contentWidthPx: number; contentHeightPx: number; lineHeightPx: number }; marginPx: number }) {
  const count = Math.floor(geo.contentHeightPx / geo.lineHeightPx);
  return (
    <Fragment>
      {Array.from({ length: count }, (_, i) => (
        <div key={i} style={{ position: "absolute", left: marginPx, top: marginPx + (i + 1) * geo.lineHeightPx, width: geo.contentWidthPx, borderBottom: "1px solid #e5e7eb" }} />
      ))}
    </Fragment>
  );
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/components/export/PrintDocument.test.tsx && node_modules/.bin/tsc --noEmit`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/components/export/PrintDocument.tsx frontend/src/components/export/PrintDocument.test.tsx
git commit -m "feat(023-r7): PrintDocument — relayout 재사용 인쇄 페이지 렌더"
```

## Task 6: PDF export 결선 + PrintOverlay (PoC 게이트)

`onExportPdf` → 챕터 수집 → `mergeChaptersForPrint` → `PrintOverlay`(PrintDocument 마운트 후 `window.print()`, afterprint 언마운트).

**Files:**
- Create: `frontend/src/components/export/PrintOverlay.tsx`
- Modify: `frontend/src/components/b/BStudioShell.tsx:535-542`
- Test: `frontend/src/components/export/PrintOverlay.test.tsx`

- [ ] **Step 1: 실패하는 테스트 작성**

```tsx
// frontend/src/components/export/PrintOverlay.test.tsx
import { render, screen } from "@testing-library/react";
import { describe, it, expect, vi, beforeEach } from "vitest";
import { PrintOverlay } from "./PrintOverlay";
import type { DocModel } from "@/components/custom-editor/model";

const models: DocModel[] = [{ buffer: "본문", blockAttrs: [{ type: "paragraph" }], markRuns: [[{ len: 2, mask: 0 }]] }];

describe("PrintOverlay", () => {
  beforeEach(() => { window.print = vi.fn(); });
  it("마운트 시 window.print 를 호출한다", () => {
    render(<PrintOverlay models={models} paperSize="A4" lined={false} onDone={vi.fn()} />);
    expect(window.print).toHaveBeenCalledTimes(1);
  });
  it("본문을 렌더한다", () => {
    render(<PrintOverlay models={models} paperSize="A4" lined={false} onDone={vi.fn()} />);
    expect(screen.getByText("본문")).toBeInTheDocument();
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/components/export/PrintOverlay.test.tsx`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: PrintOverlay 구현**

```tsx
// frontend/src/components/export/PrintOverlay.tsx
"use client";
import { useEffect } from "react";
import { PrintDocument } from "./PrintDocument";
import type { PaperSize as LayoutPaperSize } from "@/components/editor/pageLayout";
import type { DocModel } from "@/components/custom-editor/model";

type Props = { models: DocModel[]; paperSize: LayoutPaperSize; lined: boolean; onDone: () => void };

/** 인쇄 문서를 마운트하고 즉시 window.print() 를 띄운 뒤, afterprint 에서 onDone(언마운트)을 호출. */
export function PrintOverlay({ models, paperSize, lined, onDone }: Props) {
  useEffect(() => {
    const after = () => onDone();
    window.addEventListener("afterprint", after);
    window.print();
    return () => window.removeEventListener("afterprint", after);
  }, [onDone]);

  return <PrintDocument models={models} paperSize={paperSize} lined={lined} />;
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/components/export/PrintOverlay.test.tsx`
Expected: PASS.

- [ ] **Step 5: BStudioShell 결선**

`BStudioShell.tsx:535-542` 의 ExportDialog 마운트 블록을 교체. 상단에 import·state 추가:
```tsx
import { useState } from "react"; // 이미 있으면 생략
import { collectChapters } from "@/lib/export/collectChapters";
import { getDocument } from "@/lib/api/document";
import { mergeChaptersForPrint } from "@/lib/export/mergeChapters";
import { PrintOverlay } from "@/components/export/PrintOverlay";
import type { DocModel } from "@/components/custom-editor/model";
import type { ExportRequest } from "@/components/export/ExportDialog";

// 컴포넌트 내부 state
const [printModels, setPrintModels] = useState<DocModel[] | null>(null);
const [printOpts, setPrintOpts] = useState<{ lined: boolean }>({ lined: false });

const handleExportPdf = async (req: ExportRequest) => {
  setExportOpen(false);
  const chaptersData = await collectChapters(req.orderedIds, getDocument);
  setPrintOpts({ lined: req.lined });
  setPrintModels(mergeChaptersForPrint(chaptersData, req.joinMode));
};
```

마운트 블록:
```tsx
{exportOpen && (
  <ExportDialog
    open
    chapters={chapters}
    paperSize={paperSize}
    onExportPdf={handleExportPdf}
    onExportWord={() => { /* Task 12 에서 결선 */ }}
    onClose={() => setExportOpen(false)}
  />
)}
{printModels && (
  <PrintOverlay
    models={printModels}
    paperSize={paperSize}
    lined={printOpts.lined}
    onDone={() => setPrintModels(null)}
  />
)}
```

- [ ] **Step 6: 게이트 — 타입·빌드·전체 테스트**

Run: `cd frontend && node_modules/.bin/tsc --noEmit && node_modules/.bin/vitest run src/lib/export src/components/export src/components/custom-editor && pnpm build`
Expected: 전부 PASS.

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/components/export/PrintOverlay.tsx frontend/src/components/export/PrintOverlay.test.tsx frontend/src/components/b/BStudioShell.tsx
git commit -m "feat(023-r7): PDF export 결선 — collect→merge→PrintOverlay→window.print"
```

- [ ] **🚦 PoC 게이트 (사용자 dogfooding — 정지)**

Claude 는 인쇄 결과를 시각 검증할 수 없다. 사용자가 실제 작품에서 PDF 내보내기를 실행해 다음을 확인:
1. 페이지 컨테이너 내용이 물리 페이지를 **넘치지 않는가**(폰트 렌더 미세차).
2. 줄·페이지 분할이 화면 에디터와 **정합하는가**.
3. 합본 모드 3종(새페이지+제목/연속+제목/본문만)이 의도대로 나오는가.
4. 한글이 깨지지 않는가.

dogfooding 환경: 풀스택. 계정 `custom-r1@writenote.local / dogfood1234`. `/b/library` 에서 실제 작품 진입. (백엔드 health 503 은 메일 인디케이터 탓 — 기능 무관.) **통과 전 Phase 2 진입 보류.**

---

# Phase 2 — 워드 백엔드 (HWPX/DOCX)

## Task 7: 프론트 `buildExportDoc` 변환 (DocModel → ExportDoc DTO)

**Files:**
- Modify: `frontend/src/lib/export/exportDoc.ts` (Task 1 타입 파일에 함수 추가)
- Test: `frontend/src/lib/export/exportDoc.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// frontend/src/lib/export/exportDoc.test.ts
import { describe, it, expect } from "vitest";
import { docModelToExportBlocks, buildExportDoc } from "./exportDoc";
import type { DocModel } from "@/components/custom-editor/model";
import { MARK } from "@/components/custom-editor/model";
import type { CollectedChapter } from "./collectChapters";

describe("docModelToExportBlocks", () => {
  it("heading·paragraph·마크를 ExportBlock 으로 변환한다", () => {
    const model: DocModel = {
      buffer: "제목\n굵은글",
      blockAttrs: [{ type: "heading", level: 1 }, { type: "paragraph" }],
      markRuns: [[{ len: 2, mask: 0 }], [{ len: 3, mask: MARK.bold }]],
    };
    const blocks = docModelToExportBlocks(model);
    expect(blocks[0]).toMatchObject({ type: "heading", level: 1, text: "제목" });
    expect(blocks[1]).toMatchObject({ type: "paragraph", text: "굵은글" });
    expect(blocks[1].marks).toEqual([{ start: 0, end: 3, bold: true }]);
  });

  it("U+2028 소프트 줄바꿈을 \\n 으로 정규화한다", () => {
    const model: DocModel = { buffer: "가 나", blockAttrs: [{ type: "paragraph" }], markRuns: [[{ len: 3, mask: 0 }]] };
    expect(docModelToExportBlocks(model)[0].text).toBe("가\n나");
  });
});

describe("buildExportDoc", () => {
  it("paperSize·joinMode·챕터 제목을 담는다", () => {
    const pm = JSON.stringify({ type: "doc", content: [{ type: "paragraph", content: [{ type: "text", text: "본문" }] }] });
    const chapters: CollectedChapter[] = [{ id: 1, title: "1장", bodyJson: pm }];
    const doc = buildExportDoc(chapters, "A4", "page-title");
    expect(doc.paperSize).toBe("A4");
    expect(doc.joinMode).toBe("page-title");
    expect(doc.chapters[0].title).toBe("1장");
    expect(doc.chapters[0].blocks[0]).toMatchObject({ type: "paragraph", text: "본문" });
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/lib/export/exportDoc.test.ts`
Expected: FAIL — 함수 없음.

- [ ] **Step 3: 함수 추가 (exportDoc.ts 타입 블록 아래)**

```ts
// frontend/src/lib/export/exportDoc.ts — 타입 아래 추가
import { blockRuns, SOFT_BREAK, type DocModel, type MarkRun, MARK } from "@/components/custom-editor/model";
import { pmJsonToModel } from "@/components/custom-editor/pmConvert";
import type { CollectedChapter } from "./collectChapters";

function runsToMarks(runs: MarkRun[]): ExportMark[] {
  const marks: ExportMark[] = [];
  let pos = 0;
  for (const run of runs) {
    if (run.mask !== 0) {
      marks.push({
        start: pos, end: pos + run.len,
        ...(run.mask & MARK.bold ? { bold: true } : {}),
        ...(run.mask & MARK.italic ? { italic: true } : {}),
        ...(run.mask & MARK.underline ? { underline: true } : {}),
        ...(run.mask & MARK.strike ? { strike: true } : {}),
      });
    }
    pos += run.len;
  }
  return marks;
}

/** DocModel 의 각 블록을 ExportBlock 으로. U+2028 → \n 정규화(offset 은 정규화 후에도 동일 길이라 불변). */
export function docModelToExportBlocks(model: DocModel): ExportBlock[] {
  const segs = model.buffer.split("\n");
  return segs.map((seg, i) => {
    const attr = model.blockAttrs[i] ?? { type: "paragraph" as const };
    const runs = blockRuns(model, i);
    const text = seg.split(SOFT_BREAK).join("\n");
    const base = { text, marks: runsToMarks(runs) };
    switch (attr.type) {
      case "heading": return { type: "heading", level: attr.level, ...base };
      case "blockquote": return { type: "blockquote", ...base };
      case "listItem": return { type: "listItem", listKind: attr.listKind, depth: attr.depth, ...base };
      case "hr": return { type: "hr", text: "", marks: [] };
      default: return { type: "paragraph", ...base };
    }
  });
}

/** 수집 챕터들을 백엔드 워드 생성용 ExportDoc DTO 로. */
export function buildExportDoc(chapters: CollectedChapter[], paperSize: string, joinMode: JoinMode): ExportDoc {
  return {
    paperSize, joinMode,
    chapters: chapters.map((c) => ({ title: c.title, blocks: docModelToExportBlocks(pmJsonToModel(c.bodyJson)) })),
  };
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/lib/export/exportDoc.test.ts && node_modules/.bin/tsc --noEmit`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add frontend/src/lib/export/exportDoc.ts frontend/src/lib/export/exportDoc.test.ts
git commit -m "feat(023-r7): buildExportDoc — DocModel→ExportDoc DTO 변환"
```

## Task 8: 백엔드 의존성 + ExportDoc DTO

**Files:**
- Modify: `backend/build.gradle.kts:24-41`
- Create: `backend/src/main/kotlin/com/writenote/model/request/ExportRequest.kt`

- [ ] **Step 1: build.gradle.kts 의존성**

`dependencies { }` 블록(라인 24-41)에서:
- `testImplementation("kr.dogfoot:hwpxlib:1.0.5")`(라인 41) 를 `implementation("kr.dogfoot:hwpxlib:1.0.5")` 로 변경.
- 추가: `implementation("org.apache.poi:poi-ooxml:5.3.0")`

- [ ] **Step 2: 의존성 해석 확인**

Run: `cd backend && ./gradlew dependencies --configuration runtimeClasspath -q | grep -E "hwpxlib|poi-ooxml"`
Expected: 두 라이브러리가 runtimeClasspath 에 나타남.

- [ ] **Step 3: DTO 생성**

```kotlin
// backend/src/main/kotlin/com/writenote/model/request/ExportRequest.kt
package com.writenote.model.request

import jakarta.validation.constraints.NotEmpty

data class ExportMarkDto(
    val start: Int,
    val end: Int,
    val bold: Boolean = false,
    val italic: Boolean = false,
    val underline: Boolean = false,
    val strike: Boolean = false,
)

data class ExportBlockDto(
    val type: String, // paragraph | heading | blockquote | listItem | hr
    val level: Int? = null,
    val listKind: String? = null,
    val depth: Int? = null,
    val text: String = "",
    val marks: List<ExportMarkDto> = emptyList(),
)

data class ExportChapterDto(
    val title: String,
    val blocks: List<ExportBlockDto> = emptyList(),
)

data class ExportRequest(
    val paperSize: String,
    val joinMode: String, // page-title | inline-title | body-only
    @field:NotEmpty(message = "chapters must not be empty")
    val chapters: List<ExportChapterDto>,
)
```

- [ ] **Step 4: 컴파일 확인 + 커밋**

Run: `cd backend && ./gradlew compileKotlin -q`
Expected: 성공.

```bash
git add backend/build.gradle.kts backend/src/main/kotlin/com/writenote/model/request/ExportRequest.kt
git commit -m "feat(023-r7): 백엔드 export 의존성(POI·hwpxlib) + ExportRequest DTO"
```

## Task 9: `DocxExportService` (Apache POI)

ExportRequest → .docx ByteArray. joinMode 처리: `page-title`=챕터마다 제목 heading + 첫 챕터 외 페이지 나눔(`addBreak(PAGE)`), `inline-title`=제목 heading 삽입(페이지 안 나눔), `body-only`=본문만.

**Files:**
- Create: `backend/src/main/kotlin/com/writenote/service/DocxExportService.kt`
- Test: `backend/src/test/kotlin/com/writenote/service/DocxExportServiceTest.kt`

- [ ] **Step 1: 실패하는 테스트 작성**

```kotlin
// backend/src/test/kotlin/com/writenote/service/DocxExportServiceTest.kt
package com.writenote.service

import com.writenote.model.request.ExportBlockDto
import com.writenote.model.request.ExportChapterDto
import com.writenote.model.request.ExportRequest
import org.apache.poi.xwpf.usermodel.XWPFDocument
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.DisplayName
import org.junit.jupiter.api.Test
import java.io.ByteArrayInputStream

class DocxExportServiceTest {
    private val service = DocxExportService()

    @Test
    @DisplayName("단락 텍스트를 담은 .docx 를 생성한다")
    fun `generates docx with paragraph text`() {
        val req = ExportRequest(
            paperSize = "A4", joinMode = "body-only",
            chapters = listOf(ExportChapterDto("1장", listOf(ExportBlockDto(type = "paragraph", text = "안녕하세요")))),
        )
        val bytes = service.generate(req)

        XWPFDocument(ByteArrayInputStream(bytes)).use { doc ->
            val allText = doc.paragraphs.joinToString("\n") { it.text }
            assertThat(allText).contains("안녕하세요")
        }
    }

    @Test
    @DisplayName("page-title 모드는 챕터 제목을 포함한다")
    fun `page-title includes chapter title`() {
        val req = ExportRequest(
            paperSize = "A4", joinMode = "page-title",
            chapters = listOf(ExportChapterDto("프롤로그", listOf(ExportBlockDto(type = "paragraph", text = "본문")))),
        )
        val bytes = service.generate(req)
        XWPFDocument(ByteArrayInputStream(bytes)).use { doc ->
            assertThat(doc.paragraphs.joinToString("\n") { it.text }).contains("프롤로그")
        }
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd backend && ./gradlew test --tests "com.writenote.service.DocxExportServiceTest" -q`
Expected: FAIL — `DocxExportService` 없음.

- [ ] **Step 3: 구현**

```kotlin
// backend/src/main/kotlin/com/writenote/service/DocxExportService.kt
package com.writenote.service

import com.writenote.model.request.ExportBlockDto
import com.writenote.model.request.ExportChapterDto
import com.writenote.model.request.ExportRequest
import org.apache.poi.xwpf.usermodel.BreakType
import org.apache.poi.xwpf.usermodel.UnderlinePatterns
import org.apache.poi.xwpf.usermodel.XWPFDocument
import org.apache.poi.xwpf.usermodel.XWPFParagraph
import org.springframework.stereotype.Service
import java.io.ByteArrayOutputStream

/**
 * ExportRequest → .docx(ByteArray). Apache POI XWPF.
 * joinMode: page-title(제목 heading + 챕터 사이 페이지 나눔) / inline-title(제목 heading) / body-only(본문만).
 */
@Service
class DocxExportService {
    fun generate(req: ExportRequest): ByteArray {
        XWPFDocument().use { doc ->
            req.chapters.forEachIndexed { ci, chapter ->
                if (req.joinMode == "page-title" && ci > 0) {
                    doc.createParagraph().createRun().addBreak(BreakType.PAGE)
                }
                if (req.joinMode != "body-only") {
                    addTitle(doc, chapter)
                }
                chapter.blocks.forEach { block -> addBlock(doc, block) }
            }
            ByteArrayOutputStream().use { out ->
                doc.write(out)
                return out.toByteArray()
            }
        }
    }

    private fun addTitle(doc: XWPFDocument, chapter: ExportChapterDto) {
        val p = doc.createParagraph()
        p.style = "Heading1"
        p.createRun().setText(chapter.title)
    }

    private fun addBlock(doc: XWPFDocument, block: ExportBlockDto) {
        if (block.type == "hr") {
            doc.createParagraph().createRun().setText("───")
            return
        }
        val p = doc.createParagraph()
        if (block.type == "heading" && block.level != null) {
            p.style = "Heading${block.level}"
        }
        if (block.type == "blockquote") p.indentationLeft = 720 // 0.5"
        addRuns(p, block)
    }

    /** marks 구간으로 text 를 잘라 run 마다 스타일 적용. \n(소프트 줄바꿈) → addBreak(). */
    private fun addRuns(p: XWPFParagraph, block: ExportBlockDto) {
        val text = block.text
        if (text.isEmpty()) { p.createRun(); return }
        val boundaries = sortedSetOf(0, text.length)
        block.marks.forEach { boundaries.add(it.start); boundaries.add(it.end) }
        val points = boundaries.toList()
        for (i in 0 until points.size - 1) {
            val s = points[i]
            val e = points[i + 1]
            val mark = block.marks.firstOrNull { it.start <= s && e <= it.end }
            val segment = text.substring(s, e)
            // \n 단위로 쪼개 addBreak 삽입
            val lines = segment.split("\n")
            lines.forEachIndexed { li, line ->
                val run = p.createRun()
                if (mark != null) {
                    run.isBold = mark.bold
                    run.isItalic = mark.italic
                    if (mark.underline) run.underline = UnderlinePatterns.SINGLE
                    run.isStrikeThrough = mark.strike
                }
                run.setText(line)
                if (li < lines.size - 1) run.addBreak()
            }
        }
    }
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd backend && ./gradlew test --tests "com.writenote.service.DocxExportServiceTest" -q`
Expected: PASS.

- [ ] **Step 5: ktlint + 커밋**

Run: `cd backend && ./gradlew ktlintMainSourceSetCheck ktlintTestSourceSetCheck -q` (실패 시 `./gradlew ktlintFormat`)
```bash
git add backend/src/main/kotlin/com/writenote/service/DocxExportService.kt backend/src/test/kotlin/com/writenote/service/DocxExportServiceTest.kt
git commit -m "feat(023-r7): DocxExportService — POI XWPF .docx 생성"
```

## Task 10: `HwpxExportService` (hwpxlib 스파이크 승격)

ExportRequest → .hwpx ByteArray. 스파이크(`HwpxSpikeTest.kt`)의 패턴 승격 — `BlankFileMaker.make()` + 단락별 `addNewPara`/`addNewRun`/`addNewT` + bold/italic CharPr + 한글 폰트. 제목·페이지 나눔은 스파이크 검증 범위(⚠️) 내에서 최대 적용.

**Files:**
- Create: `backend/src/main/kotlin/com/writenote/service/HwpxExportService.kt`
- Test: `backend/src/test/kotlin/com/writenote/service/HwpxExportServiceTest.kt`

- [ ] **Step 1: 실패하는 테스트 작성**

```kotlin
// backend/src/test/kotlin/com/writenote/service/HwpxExportServiceTest.kt
package com.writenote.service

import com.writenote.model.request.ExportBlockDto
import com.writenote.model.request.ExportChapterDto
import com.writenote.model.request.ExportRequest
import org.assertj.core.api.Assertions.assertThat
import org.junit.jupiter.api.DisplayName
import org.junit.jupiter.api.Test

class HwpxExportServiceTest {
    private val service = HwpxExportService()

    @Test
    @DisplayName("hwpx ByteArray 를 생성한다(ZIP 시그니처 PK)")
    fun `generates non-empty hwpx with zip signature`() {
        val req = ExportRequest(
            paperSize = "A4", joinMode = "body-only",
            chapters = listOf(ExportChapterDto("1장", listOf(ExportBlockDto(type = "paragraph", text = "안녕하세요 나래 노트")))),
        )
        val bytes = service.generate(req)
        assertThat(bytes).isNotEmpty()
        // .hwpx 는 OOXML 류 ZIP — 첫 2바이트 'PK'
        assertThat(bytes[0]).isEqualTo('P'.code.toByte())
        assertThat(bytes[1]).isEqualTo('K'.code.toByte())
    }
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd backend && ./gradlew test --tests "com.writenote.service.HwpxExportServiceTest" -q`
Expected: FAIL — `HwpxExportService` 없음.

- [ ] **Step 3: 구현 (스파이크 패턴 승격, ByteArray 출력)**

```kotlin
// backend/src/main/kotlin/com/writenote/service/HwpxExportService.kt
package com.writenote.service

import com.writenote.model.request.ExportBlockDto
import com.writenote.model.request.ExportRequest
import kr.dogfoot.hwpxlib.`object`.HWPXFile
import kr.dogfoot.hwpxlib.`object`.content.section_xml.SectionXMLFile
import kr.dogfoot.hwpxlib.tool.blankfilemaker.BlankFileMaker
import kr.dogfoot.hwpxlib.writer.HWPXWriter
import org.springframework.stereotype.Service
import java.io.ByteArrayOutputStream

/**
 * ExportRequest → .hwpx(ByteArray). kr.dogfoot:hwpxlib.
 * 스파이크(HwpxSpikeTest) 패턴 승격. 제목 styleIDRef·용지·bold/italic 매핑은 스파이크 검증 범위(한컴 렌더 dogfooding 확인).
 */
@Service
class HwpxExportService {
    fun generate(req: ExportRequest): ByteArray {
        val hwpx = BlankFileMaker.make()
        registerBoldItalicCharPr(hwpx)
        val section = hwpx.sectionXMLFileList().get(0)

        var paraId = 1
        req.chapters.forEach { chapter ->
            if (req.joinMode != "body-only") {
                addPara(section, paraId++, "0", chapter.title, styleRef = "1")
            }
            chapter.blocks.forEach { block ->
                addPara(section, paraId++, charPrFor(block), blockText(block), styleRef = headingStyle(block))
            }
        }
        return write(hwpx)
    }

    private fun blockText(block: ExportBlockDto): String =
        if (block.type == "hr") "───" else block.text.replace("\n", " ")

    private fun headingStyle(block: ExportBlockDto): String =
        if (block.type == "heading" && block.level != null) block.level.toString() else "0"

    /** 블록 전체에 단일 mark 가 걸리면 그 charPr, 아니면 기본(0). (run 분할 정밀화는 후속.) */
    private fun charPrFor(block: ExportBlockDto): String {
        val m = block.marks.firstOrNull() ?: return "0"
        return when {
            m.bold && m.italic -> "12"
            m.bold -> "10"
            m.italic -> "11"
            else -> "0"
        }
    }

    private fun registerBoldItalicCharPr(hwpx: HWPXFile) {
        val charProps = hwpx.headerXMLFile()?.refList()?.charProperties() ?: return
        charProps.addNew().apply { idAnd("10").heightAnd(1000); createBold() }
        charProps.addNew().apply { idAnd("11").heightAnd(1000); createItalic() }
        charProps.addNew().apply { idAnd("12").heightAnd(1000); createBold(); createItalic() }
    }

    private fun addPara(section: SectionXMLFile, id: Int, charPrRef: String, text: String, styleRef: String) {
        val para = section.addNewPara()
        para.idAnd(id.toString()).paraPrIDRefAnd("0").styleIDRefAnd(styleRef).pageBreakAnd(false).columnBreakAnd(false)
        val run = para.addNewRun()
        run.charPrIDRefAnd(charPrRef)
        run.addNewT().addText(text)
    }

    private fun write(hwpx: HWPXFile): ByteArray =
        ByteArrayOutputStream().use { out ->
            HWPXWriter.toStream(hwpx, out)
            out.toByteArray()
        }
}
```

> **검증 노트:** `HWPXWriter.toStream(hwpxFile, OutputStream)` 시그니처는 라이브러리 버전에 따라 다를 수 있다. 구현 전 `./gradlew dependencies` 로 받은 jar 에서 `HWPXWriter` 의 public 메서드를 확인(IDE 또는 `javap`)한다. `toStream` 부재 시 임시 파일(`toFilepath`) → `readBytes()` 폴백.

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd backend && ./gradlew test --tests "com.writenote.service.HwpxExportServiceTest" -q`
Expected: PASS.

- [ ] **Step 5: ktlint + 커밋**

Run: `cd backend && ./gradlew ktlintMainSourceSetCheck ktlintTestSourceSetCheck -q`
```bash
git add backend/src/main/kotlin/com/writenote/service/HwpxExportService.kt backend/src/test/kotlin/com/writenote/service/HwpxExportServiceTest.kt
git commit -m "feat(023-r7): HwpxExportService — hwpxlib .hwpx 생성(스파이크 승격)"
```

## Task 11: `ExportController` (2 endpoint + 소유자 검증 + 파일 응답)

**Files:**
- Create: `backend/src/main/kotlin/com/writenote/controller/ExportController.kt`
- Test: `backend/src/test/kotlin/com/writenote/controller/ExportControllerWebTest.kt`

> 소유자 검증: 요청 본문에는 projectId 가 없다(챕터 본문은 프론트가 이미 수집). endpoint 는 `projectId` 를 path 로 받아 `ProjectService.requireOwnedProject(userId, projectId)` 로 소유 확인만 한 뒤 생성한다. (본문 위변조 위험은 "자기 작품 export" 특성상 낮음 — 향후 서버 측 챕터 재수집으로 강화 가능, 범위 밖.)

- [ ] **Step 1: 실패하는 통합 테스트 작성**

```kotlin
// backend/src/test/kotlin/com/writenote/controller/ExportControllerWebTest.kt
package com.writenote.controller

import com.fasterxml.jackson.databind.ObjectMapper
import com.writenote.model.request.ExportBlockDto
import com.writenote.model.request.ExportChapterDto
import com.writenote.model.request.ExportRequest
import org.junit.jupiter.api.DisplayName
import org.junit.jupiter.api.Test
import org.springframework.beans.factory.annotation.Autowired
import org.springframework.boot.test.context.SpringBootTest
import org.springframework.http.MediaType
import org.springframework.test.context.ActiveProfiles
import org.springframework.test.web.servlet.MockMvc
import org.springframework.test.web.servlet.post
import org.springframework.transaction.annotation.Transactional

@SpringBootTest
@org.springframework.boot.test.autoconfigure.web.servlet.AutoConfigureMockMvc
@ActiveProfiles("test")
@Transactional
class ExportControllerWebTest @Autowired constructor(
    private val mockMvc: MockMvc,
    private val objectMapper: ObjectMapper,
    // 기존 테스트의 인증 헬퍼/시드 유틸 주입 — AuthControllerWebTest 패턴 따름
) {
    @Test
    @DisplayName("인증 없이 docx export 는 401")
    fun `docx export requires auth`() {
        val req = ExportRequest("A4", "body-only", listOf(ExportChapterDto("1장", listOf(ExportBlockDto(type = "paragraph", text = "본문")))))
        mockMvc.post("/api/export/1/docx") {
            contentType = MediaType.APPLICATION_JSON
            content = objectMapper.writeValueAsString(req)
        }.andExpect { status { isUnauthorized() } }
    }

    // 인증 성공 경로(소유 작품 docx 200 + Content-Disposition)는 기존 시드/JWT 헬퍼로 작성.
    // AuthControllerWebTest 의 로그인→토큰 발급 패턴을 재사용해 Authorization 헤더를 붙인다.
}
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd backend && ./gradlew test --tests "com.writenote.controller.ExportControllerWebTest" -q`
Expected: FAIL — endpoint 없음(404, 401 아님).

- [ ] **Step 3: Controller 구현**

```kotlin
// backend/src/main/kotlin/com/writenote/controller/ExportController.kt
package com.writenote.controller

import com.writenote.auth.AuthenticatedPrincipal
import com.writenote.model.request.ExportRequest
import com.writenote.service.DocxExportService
import com.writenote.service.HwpxExportService
import com.writenote.service.ProjectService
import io.swagger.v3.oas.annotations.security.SecurityRequirement
import jakarta.validation.Valid
import org.springframework.http.HttpHeaders
import org.springframework.http.MediaType
import org.springframework.http.ResponseEntity
import org.springframework.security.core.annotation.AuthenticationPrincipal
import org.springframework.web.bind.annotation.PathVariable
import org.springframework.web.bind.annotation.PostMapping
import org.springframework.web.bind.annotation.RequestBody
import org.springframework.web.bind.annotation.RestController
import java.net.URLEncoder
import java.nio.charset.StandardCharsets

@RestController
@SecurityRequirement(name = "BearerJwt")
class ExportController(
    private val projectService: ProjectService,
    private val docxExportService: DocxExportService,
    private val hwpxExportService: HwpxExportService,
) {
    @PostMapping("/api/export/{projectId}/docx")
    fun exportDocx(
        @AuthenticationPrincipal principal: AuthenticatedPrincipal,
        @PathVariable projectId: Long,
        @Valid @RequestBody request: ExportRequest,
    ): ResponseEntity<ByteArray> {
        projectService.requireOwnedProject(principal.userId, projectId)
        return fileResponse(docxExportService.generate(request), "docx", projectId)
    }

    @PostMapping("/api/export/{projectId}/hwpx")
    fun exportHwpx(
        @AuthenticationPrincipal principal: AuthenticatedPrincipal,
        @PathVariable projectId: Long,
        @Valid @RequestBody request: ExportRequest,
    ): ResponseEntity<ByteArray> {
        projectService.requireOwnedProject(principal.userId, projectId)
        return fileResponse(hwpxExportService.generate(request), "hwpx", projectId)
    }

    private fun fileResponse(bytes: ByteArray, ext: String, projectId: Long): ResponseEntity<ByteArray> {
        val filename = URLEncoder.encode("export-$projectId.$ext", StandardCharsets.UTF_8).replace("+", "%20")
        return ResponseEntity.ok()
            .header(HttpHeaders.CONTENT_DISPOSITION, "attachment; filename*=UTF-8''$filename")
            .contentType(MediaType.APPLICATION_OCTET_STREAM)
            .body(bytes)
    }
}
```

> 작업 전 확인: `AuthenticatedPrincipal` 의 사용자 식별 프로퍼티명(`userId` 인지 `id` 인지) 을 `grep -n "class AuthenticatedPrincipal" -A5 backend/src/main/kotlin/com/writenote/auth/` 로 확인해 정합시킨다.

- [ ] **Step 4: 테스트 통과 확인 + ktlint**

Run: `cd backend && ./gradlew test --tests "com.writenote.controller.ExportControllerWebTest" ktlintMainSourceSetCheck ktlintTestSourceSetCheck -q`
Expected: PASS.

- [ ] **Step 5: 커밋**

```bash
git add backend/src/main/kotlin/com/writenote/controller/ExportController.kt backend/src/test/kotlin/com/writenote/controller/ExportControllerWebTest.kt
git commit -m "feat(023-r7): ExportController — /api/export/{projectId}/{docx,hwpx}"
```

## Task 12: 프론트 워드 export API + blob 다운로드 결선 (dogfooding 게이트)

**Files:**
- Create: `frontend/src/lib/api/export.ts`
- Modify: `frontend/src/components/b/BStudioShell.tsx`
- Test: `frontend/src/lib/api/export.test.ts`

- [ ] **Step 1: 실패하는 테스트 작성**

```ts
// frontend/src/lib/api/export.test.ts
import { describe, it, expect, vi, beforeEach } from "vitest";
import { exportWord } from "./export";
import type { ExportDoc } from "@/lib/export/exportDoc";

const doc: ExportDoc = { paperSize: "A4", joinMode: "body-only", chapters: [{ title: "1장", blocks: [] }] };

describe("exportWord", () => {
  beforeEach(() => {
    vi.stubGlobal("fetch", vi.fn(async () => new Response(new Blob(["x"]), { status: 200, headers: { "Content-Disposition": "attachment; filename*=UTF-8''export-9.docx" } })));
  });
  it("projectId·format 으로 POST 하고 blob 을 반환한다", async () => {
    const { blob, filename } = await exportWord(9, "docx", doc);
    expect(fetch).toHaveBeenCalledWith(expect.stringContaining("/api/export/9/docx"), expect.objectContaining({ method: "POST" }));
    expect(blob).toBeInstanceOf(Blob);
    expect(filename).toBe("export-9.docx");
  });
});
```

- [ ] **Step 2: 테스트 실패 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/lib/api/export.test.ts`
Expected: FAIL — 모듈 없음.

- [ ] **Step 3: API 구현 (기존 client 베이스 URL·인증 헤더 패턴 따름)**

> 작업 전 `frontend/src/lib/api/document.ts` 와 `client.ts` 를 읽어 baseURL·Authorization 헤더 주입 방식을 확인하고 동일 패턴을 쓴다. 아래는 fetch 직접 사용 예시 — 프로젝트가 공용 `apiFetch` 래퍼를 쓰면 그것으로 교체(단, blob 응답 지원 확인).

```ts
// frontend/src/lib/api/export.ts
import type { ExportDoc } from "@/lib/export/exportDoc";
import { API_BASE_URL, authHeader } from "./client"; // 실제 export 명칭은 client.ts 확인 후 정합

function parseFilename(disposition: string | null, fallback: string): string {
  const m = disposition?.match(/filename\*=UTF-8''([^;]+)/);
  return m ? decodeURIComponent(m[1]) : fallback;
}

/** 워드 export — ExportDoc 을 POST 하고 응답 blob + 파일명을 반환. */
export async function exportWord(projectId: number, format: "hwpx" | "docx", doc: ExportDoc): Promise<{ blob: Blob; filename: string }> {
  const res = await fetch(`${API_BASE_URL}/api/export/${projectId}/${format}`, {
    method: "POST",
    headers: { "Content-Type": "application/json", ...authHeader() },
    body: JSON.stringify(doc),
  });
  if (!res.ok) throw new Error(`export failed: ${res.status}`);
  const blob = await res.blob();
  return { blob, filename: parseFilename(res.headers.get("Content-Disposition"), `export-${projectId}.${format}`) };
}

/** blob 을 브라우저 다운로드로 트리거. */
export function downloadBlob(blob: Blob, filename: string): void {
  const url = URL.createObjectURL(blob);
  const a = document.createElement("a");
  a.href = url;
  a.download = filename;
  a.click();
  URL.revokeObjectURL(url);
}
```

- [ ] **Step 4: 테스트 통과 확인**

Run: `cd frontend && node_modules/.bin/vitest run src/lib/api/export.test.ts`
Expected: PASS.

- [ ] **Step 5: BStudioShell `onExportWord` 결선**

```tsx
// BStudioShell.tsx — import 추가
import { buildExportDoc } from "@/lib/export/exportDoc";
import { exportWord, downloadBlob } from "@/lib/api/export";

// 핸들러
const handleExportWord = async (format: "hwpx" | "docx", req: ExportRequest) => {
  setExportOpen(false);
  const chaptersData = await collectChapters(req.orderedIds, getDocument);
  const doc = buildExportDoc(chaptersData, paperSize, req.joinMode);
  const { blob, filename } = await exportWord(projectId, format, doc);
  downloadBlob(blob, filename);
};
```
ExportDialog 마운트의 `onExportWord={() => {}}` 를 `onExportWord={handleExportWord}` 로 교체. (`projectId` 가 BStudioShell scope 에 있는지 확인 — 없으면 props/query 에서 가져옴.)

- [ ] **Step 6: 게이트 — 프론트·백엔드 전체**

Run:
```
cd frontend && node_modules/.bin/tsc --noEmit && node_modules/.bin/vitest run src/lib src/components/export && pnpm build
cd backend && ./gradlew ktlintMainSourceSetCheck ktlintTestSourceSetCheck checkstyleMain test build
```
Expected: 전부 GREEN.

- [ ] **Step 7: 커밋**

```bash
git add frontend/src/lib/api/export.ts frontend/src/lib/api/export.test.ts frontend/src/components/b/BStudioShell.tsx
git commit -m "feat(023-r7): 워드 export 결선 — buildExportDoc→POST→blob 다운로드"
```

- [ ] **🚦 dogfooding 게이트 (사용자 — 정지)**

사용자가 실제 작품에서 HWPX·DOCX 내보내기를 실행해 확인:
1. 생성된 `.docx` 를 **MS Word / 한글**에서 열어 제목·문단·마크·합본 구분이 맞는가.
2. 생성된 `.hwpx` 를 **한컴오피스**에서 열어 한글·제목 스타일(styleIDRef 1~3)·bold/italic 이 의도대로 렌더되는가(스파이크 ⚠️ 항목 실측).
3. 합본 모드 3종이 워드에서도 의도대로인가.

문제 발견 시 §11(관찰→확정→수정) 적용. HWPX 제목 스타일이 한컴에서 안 먹으면 styleIDRef 매핑 재조정(후속).

---

## Self-Review

**Spec coverage:**
- §2 결정1 포맷 3개 → Task 1(다이얼로그)·6(PDF)·9·10·11(워드) ✅
- §2 결정2 PDF 브라우저 인쇄 → Task 3·4·5·6 ✅
- §2 결정3 워드 백엔드 단일 → Task 8·9·10 ✅
- §2 결정4 ExportDoc DTO → Task 7·8 ✅
- §2 결정5 챕터 합본 선택형 → Task 1(셀렉트)·3(PDF)·9·10(워드 joinMode) ✅
- §2 결정6 줄노트 토글 → Task 1·5(LinedBackground) ✅
- §3 아키텍처 / §4 컴포넌트 / §5 DTO / §6 다이얼로그 / §7 Phase / §8 테스트 → 전 Task 매핑 ✅
- §9 위험(PDF 넘침=PoC 게이트 Task 6, HWPX 한컴=dogfooding Task 12, 인증=Task 11) ✅

**Placeholder scan:** `printLayout.tsx`(Task 4)의 `IMG_SRC`/`ParsedBlock`/`relayout`/`renderRuns` 본문은 "CustomEditor.tsx 의 해당 라인 그대로"로 명시(발췌 추출이라 원본 복제가 정확) — 추출 작업의 성격상 라인 지정이 적절. 그 외 모든 코드 step 은 완전한 코드 포함.

**Type consistency:** `ExportRequest`(ts `ExportDoc` ↔ kotlin `ExportRequest`) 필드명 일치(paperSize/joinMode/chapters/title/blocks/type/level/listKind/depth/text/marks). `ExportRequest`(프론트 다이얼로그 req: orderedIds/lined/joinMode) ↔ `ExportDoc`(백엔드 전송) 은 별개 타입(다이얼로그 req 는 수집 입력, ExportDoc 는 변환 결과) — 혼동 없도록 명명 구분 유지.

**미해결(구현 중 확인):** `HWPXWriter.toStream` 시그니처(Task 10 노트), `AuthenticatedPrincipal.userId` 프로퍼티명(Task 11 노트), `client.ts` 의 baseURL/authHeader export 명(Task 12 노트), `BStudioShell` 의 `projectId` scope. 각 Task 에 확인 절차 명시.
