import { PreferencesSections } from "@/components/mypage/PreferencesSections";

/**
 * 마이페이지 환경설정 섹션 (037) — 테마·기본 용지·일일 목표 (기존 /settings 흡수).
 */
export default function MypageSettingsPage() {
    return <PreferencesSections />;
}
