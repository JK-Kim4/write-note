spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/writenote
    username: writenote
    password: writenote-local-dev

app:
  cors:
    allowed-origins: http://localhost:3000
  admin:
    email: ${ADMIN_EMAIL:admin@writenote.local}
  auth:
    jwt:
      secret: ${JWT_SECRET:d24tbG9jYWwtZGV2LWp3dC0zMmJ5dGUtc2VjcmV0LXg=}
  mail:
    mode: log
