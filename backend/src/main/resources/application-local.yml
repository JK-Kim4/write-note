spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/writenote
    username: writenote
    password: writenote-local-dev

app:
  cors:
    allowed-origins: http://localhost:3000
  auth:
    jwt:
      secret: ${JWT_SECRET:local-dev-secret-please-replace-with-base64-32byte-string-AAAAAAAA}
  mail:
    mode: log
