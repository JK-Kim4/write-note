spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/writenote
    username: writenote
    password: writenote-local-dev
    hikari:
      maximum-pool-size: 3
  jpa:
    properties:
      hibernate:
        generate_statistics: true
  test:
    database:
      replace: none
  security:
    oauth2:
      client:
        registration:
          kakao:
            client-id: test-placeholder-not-used
            client-secret: test-placeholder-not-used
            redirect-uri: http://localhost:0/api/auth/oauth/kakao/callback
            scope:
              - profile_nickname
              - account_email

app:
  cors:
    allowed-origins: http://localhost:3000
  admin:
    email: admin@writenote.test
  auth:
    jwt:
      secret: d24tdGVzdC1kZXRlcm1pbmlzdGljLWp3dC0zMmJ5dGU=
  frontend:
    base-url: http://localhost:3000
  mail:
    mode: log
    base-url: http://localhost:8080
  crypto:
    # 테스트 전용 고정 KEK (비밀 아님 — 테스트 결정론성을 위해 고정값 사용, 정확히 32B)
    master-key: ct8w0Qt1ZC7m8r+PcjYwMKGTX9XY8nJm4ZFKhGyaIh0=
